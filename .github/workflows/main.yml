name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        apt update && apt install -y gpg pbuilder debootstrap devscripts python-apt reprepro
