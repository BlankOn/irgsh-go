name: Add issues to BlankOn Project

on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - name: Add issue to project
        uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/orgs/BlankOn/projects/4
          github-token: ${{ secrets.PROJECT_PAT }}
